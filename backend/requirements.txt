redis>=5.0.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
email-validator>=2.0
fastapi==0.141.1
# Pinned explicitly, not left to fastapi's dependency range. fastapi >= 0.135
# declares only `starlette>=0.46.0` with no upper bound, so an unpinned build
# would silently pull whatever starlette is newest that day — the ASGI layer
# under every request, chosen non-reproducibly. This is the version the test
# suite is green against.
#
# Upgraded from starlette 0.41.3 (which fastapi 0.115.6 pinned), where
# pip-audit reported 8 advisories including the request-smuggling and
# response-body cache-confusion family. Clearing all of them required
# starlette >= 1.3.1, which in turn required fastapi >= 0.135.
starlette==1.6.0
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.2.2
httpx==0.28.1
h2>=4.1.0
pypdf>=4.0.0
requests>=2.32.0
python-multipart>=0.0.9
pandas==2.2.3
numpy==2.2.1
groq==0.13.1
openai>=1.0.0
scikit-learn==1.6.0
xgboost==2.1.3
lightgbm==4.6.0
feedparser==6.0.11
vaderSentiment==3.3.2
slowapi==0.1.9
sentry-sdk[fastapi]==2.20.0
pyotp==2.9.0
cryptography>=42.0.0
Pillow>=10.4.0
