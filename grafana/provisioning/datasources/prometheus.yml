# Auto-provisions the Prometheus datasource on Grafana boot — without this
# file, Grafana starts as an empty shell with no datasource configured.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
